#!/bin/bash

# Wrapper script for monitor.sh
# Usage: ./monitor [command]

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the main monitor script
exec "${SCRIPT_DIR}/scripts/monitor.sh" "$@"
